spring.application.name=ioc-building
server.port=8080

management.endpoints.web.exposure.include=health,info

# In the compose stack LOGGING_FILE_NAME=/var/log/ioc-building/app.log is set so
# the log-monitor microservice can tail this service's log file.
#logging.file.name=/var/log/ioc-building/app.log

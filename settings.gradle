plugins {
    // Auto-provisions the Java 21 toolchain when the running JVM differs
    // (e.g. the code-review agent container runs Gradle on Java 25).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'ioc-building'
